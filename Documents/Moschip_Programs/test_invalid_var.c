#include<stdio.h>
int main()
{
	char x = 500;
	int u = 'a'; // ascii number of is stored in x 
	unsigned int z = -1;

	if( z > 0){
		print("hello");
	}

	printf("%d, %u, %x, %o\n",z,z,z,z);

	//	printf("%d",u);
	//printf("%o",x);

}
